<div class="page-header">
  <h1>Editing <%= link_to @session.name, [@event, @session] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [@event, @session], label: "session" %>
