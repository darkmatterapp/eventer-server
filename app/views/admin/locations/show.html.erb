<%= render "shared/notice" %>

<div class="h-event">
  <div class="page-header">
    <h1>
      <span class="p-name"><%= @location.name %></span>
    </h1>
    <p><%= link_to "EDIT", [:edit, :admin, @event, @venue, @location], class: "btn btn-default btn-xs" %></p>
    <p>For <%= link_to @event.name, [:admin, @event] %>.</p>
    <p>At <%= link_to @venue.name, [:admin, @event, @venue] %>.</p>
  </div>
</div>
