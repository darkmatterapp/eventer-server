<div class="page-header">
  <h1>Editing <%= link_to @location.name, [:admin, @event, @venue, @location] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [:admin, @event, @venue, @location], label: "location" %>
