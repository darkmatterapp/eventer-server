<div class="page-header">
  <h1>Editing <%= link_to @venue.name, [:admin, @event, @venue] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [:admin, @event, @venue], label: "venue" %>
