<%= form_for [:admin, @event, @participant] do |f| %>
  <%= render "shared/form_errors", resource: @participant %>
  <%= render "participant_fields", f: f %>
  <%= render "shared/form_actions", f: f %>
<% end %>
