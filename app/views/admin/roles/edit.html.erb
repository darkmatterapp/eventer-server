<div class="page-header">
  <h1>Editing <%= link_to @role.name, [:admin, @event, @role] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [:admin, @event, @role], label: "role" %>
