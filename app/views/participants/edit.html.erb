<div class="page-header">
  <h1>Editing <%= link_to @participant.name, [:edit, @event, @participant] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [@event, @participant], label: "participant" %>
