<div class="page-header">
  <h1>Editing <%= link_to @person.name, [:edit, @event, @person] %></h1>
</div>

<%= render "form" %>
<%= render "shared/danger_zone", resource: [@event, @person], label: "person" %>
