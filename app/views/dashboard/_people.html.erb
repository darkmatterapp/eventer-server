<dl>
  <% event.people.each do |person| %>
    <dt><%= link_to person.name, [event, person] %></dt>

    <% if person.sessions.present? %>
      <% person.sessions.each do |session| %>
        <dd><%= link_to session.name, [event, session] %></dd>
      <% end %>
    <% end %>

  <% end %>
</dl>
